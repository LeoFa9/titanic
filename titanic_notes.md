### To-do
- для непрерывных фичей сделать разбиение на бины
- в ноутбуке сначала сделать анализ и предобработку данных, затем построить модели с дефолтными параметрами, а затем уже заниматься поиском по сетке
- постараться сохранить как можно больше строк данных, все null значения обработать
- помимо всего того, что написано в чеклисте попробовать optuna
- посмотреть что можно сделать с колонкой Pclass
- из колонки Name выделить приставки типа Mr., Mrs. и тд, а затем дропнуть
- sex перевести из строкового в числовое (male - 0, female - 1)
- age разбить на бины (применять ли one-hot?)
- SibSp и Parch объединить в колонку FamilySize, затем дропнуть изначальные признаки (применять ли one-hot?)
- добавить колонку IsAlone
- fare разбить на бины (применять ли one-hot?)
- к embarked применить one-hot

### Notes
- выживаемость сильно коррелирует с женским полом и уровнем класса пассажира
- данные для валидации должны быть в таком же формате, как и для тренировки (для разных моделей разные датасеты по предобработке?)
- в [ноутбуке](https://www.kaggle.com/code/startupsci/titanic-data-science-solutions) для работы с пропущенными значениями объединяют обучающий и тестовый наборы данных и считают медиану, среднее и тд на объединенном датасете, но так делать не нужно, это ведет к лику данных, нужно обрабатывать по отдельности
- 4, 5 и 8 SibSp было только у людей из 3 класса
- переменные и логи должны иметь осознанные имена
- бэггинг применяется для того, чтобы уменьшить variance (дисперсию) базовой модели

### Questions
- для разных моделей разные датасеты по предобработке?
- как сделать так, чтобы ноутбук смог запуститься в докере? (возможная проблема с библиотеками)
- в тестовом наборе данных нет колонки с таргетом. Валидироваться при обучении надо на тренировочном наборе, а потом для понимания скора для тестового набора данных загружать сабмит?
- к порядковому Pclass нужно применять one-hot encoding?

### Solution

| Approach | CV  | CV STD | LB  | Date |
| -------- | --- | ------ | --- | ---- |
|          |     |        |     |      |

### Annotations

| №   | Annotation |
| --- | ---------- |
|     |            |

### Final Ensemble

| Model | CV  | Public LB | Private LB |
| ----- | --- | --------- | ---------- |
|       |     |           |            |

### Processed ideas
#### Good
- pass
#### Neutral
- pass
#### Bad
- pass
#### Some experimental ideas I didn't implemented
- pass